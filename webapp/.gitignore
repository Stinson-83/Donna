node_modules
dist
.env
.env.local
.env.*.local
*.log
.vite

# Capacitor / mobile
www
android/local.properties
android/keystore.properties
*.keystore
*.jks

# Capacitor copies the web build into the native projects; don't track copies
android/app/src/main/assets/public
ios/App/App/public

# Firebase config — contains project keys, regenerated per-environment.
# (paths in this file are relative to webapp/, so no leading "webapp/")
android/app/google-services.json
ios/App/App/GoogleService-Info.plist