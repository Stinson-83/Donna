"""The card system — DonnaCard payloads, persistence, surface projection,
and action resolution. Payload contract is owned by `donna-design-spec/`
(schema/card.schema.json); `models.py` here is the vendored backend twin."""
