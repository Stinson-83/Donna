"""Finance domain — the low_balance_vs_bill detector + proactive trigger (M3).
Detection is deterministic (proactive_runner §5 Layer-1 diff); the transfer
itself runs through the §10.3 L0 gate as a card-action executor."""
