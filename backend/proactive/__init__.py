"""The proactive runner — the 'she texts first' tick (proactive_runner.md).
A deterministic dispatcher that periodically runs proactive checks across users
and lets each surface a card when something needs the user. No reasoning here."""
