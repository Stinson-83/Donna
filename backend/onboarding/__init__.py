"""Onboarding — backfill the user's life from their connected accounts so the
proactive checks have real data on day one (onboarding.md). Baseline: Google
Calendar -> calendar entries + relationships (from attendees + email senders).
Deterministic; LLM distillation of goals/identity is the documented refinement."""
